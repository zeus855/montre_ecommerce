{% extends 'base.html.twig' %}

{% block title %}{{montre.titre}}{% endblock %}

{% block body %}

    <div class="container mt-5">
    <div class="row">
        <div class="col-md-6">
            <img  src="{{ asset('images/montres/' ~ montre.imageName) | imagine_filter('miniature') }}" alt="{{ montre.titre }}" class="img-responsive">
        </div>
        <div class="col-md-6">
            <h2>{{montre.titre}}</h2>
            <p class="text-muted">Prix : {{montre.prix}} €</p>
            <p>{{ montre.description }}</p>

            {# <div class="form-group">
                <label for="quantity">Quantité :</label>
                <input type="number" id="quantity" name="quantity" class="form-control" min="1" value="1">
            </div> #}

            <a href={{ path('app_ajout_panier', {slug: montre.slug}) }} class="btn btn-primary">Ajouter au Panier</a>
        </div>
    </div>

    {# <h3>Caractéristiques de la Montre :</h3>
    <ul>
        <li>Boîtier en acier inoxydable</li>
        <li>Bracelet en cuir véritable</li>
        <li>Mouvement automatique suisse</li>
        <li>Résistant à l'eau jusqu'à 50 mètres</li>
    </ul> #}
</div>


    {# <h1>{{montre.titre}}</h1>

    <a class="btn btn-primary" href="{{ path('front') }}">Retour</a>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ montre.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ montre.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ montre.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ montre.prix }} € </td>
            </tr>
            <tr>
                <th>Image_name</th>
                <td>{{ montre.imageName }}</td>
                <td><img src="{{ asset('images/montres/' ~ montre.imageName) | imagine_filter('miniature') }}" alt=""</td>

                
            </tr>

            
            <tr>
                <th>CreatedAt</th>
                <td>{{ montre.createdAt ? montre.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <a class="btn btn-success" href="{{ path('app_ajout_panier', {id:montre.id}) }}">Ajouter au panier</a>
        </tbody>
    </table> #}

{% endblock %}
