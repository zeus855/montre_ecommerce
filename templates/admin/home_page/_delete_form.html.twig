<form method="post" action="{{ path('app_admin_home_page_delete', {'id': home_page.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ home_page.id) }}">
    <button class="btn">Delete</button>
</form>
